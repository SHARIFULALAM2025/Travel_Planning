import ViewFlight from '@/Components/Explore_DestinationPages/ViewFlight/ViewFlight';
import React from 'react';

const page = ({params}) => {
    return (
      <div>
        <ViewFlight params={params} />
      </div>
    )
};

export default page;