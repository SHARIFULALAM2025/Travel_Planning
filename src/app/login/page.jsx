import Login from '@/Components/Login/Login';
import React from 'react';

const LoginPage = () => {
    return (
        <div>
            <Login></Login>
        </div>
    );
};

export default LoginPage;